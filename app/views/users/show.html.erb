<h1>Collection</h1>
<body>
  <div class=books>
    <% @favorite_books.each do |book| %>
              <%= link_to image_tag("#{book.image}"), book_path(book.id) %>
              <% end %>
  </div>            
</body>    