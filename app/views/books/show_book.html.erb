	<h1>皆のkiller</h1>
<% @book.each do |book| %>
<div class="image_area" >
  <%= link_to image_tag("#{book.image}"), book_path(book.id) %>
    <div class="title">
    TITLE: <%= book.title %>
    <div class="author">
    著者: <%= book.author %>
</div>   
<% end %>

